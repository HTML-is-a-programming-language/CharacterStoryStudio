# 아무것도 설정하지 않아도 앱은 정상 동작합니다(Demo Mode, Mock Provider만 사용, 비용 없음).
#
# 실제 이미지 생성 API(OpenAI)를 쓰려면 아래 두 값을 "모두" 채워야 합니다.
# 하나만 있으면 여전히 Mock으로 동작합니다 — 실수로 비용이 발생하지 않게 하기 위한 이중 장치입니다.
# 실제로 켜면 호출마다 OpenAI 요금이 발생합니다(정확한 금액은 OpenAI 가격 페이지를 확인하세요).

# IMAGE_PROVIDER=real
# OPENAI_API_KEY=sk-...
